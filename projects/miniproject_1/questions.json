[
    {
        "question": "What is the output of type({}) in Python?",
        "options": ["<class 'set'>", "<class 'dict'>", "<class 'object'>", "<class 'list'>"],
        "answer": 1,
        "explanation": "{} creates an empty dict, not a set. Use set() to create an empty set."
    },
    {
        "question": "Which method removes and returns the last item from a list?",
        "options": ["list.remove()", "list.delete()", "list.pop()", "list.discard()"],
        "answer": 2,
        "explanation": "list.pop() removes and returns the last element by default. Pass an index to remove a specific position."
    },
    {
        "question": "What does the 'pass' statement do in Python?",
        "options": [
            "Exits the current loop",
            "Skips to the next iteration",
            "Does nothing — acts as a placeholder",
            "Returns None from a function"
        ],
        "answer": 2,
        "explanation": "'pass' is a null statement used where syntax requires a statement but no action is needed."
    },
    {
        "question": "What is the result of 3 ** 3 in Python?",
        "options": ["6", "9", "27", "33"],
        "answer": 2,
        "explanation": "** is the exponentiation operator. 3 ** 3 = 3 raised to the power of 3 = 27."
    },
    {
        "question": "What does enumerate() return when iterating?",
        "options": [
            "A list of values",
            "An iterator of (index, value) pairs",
            "A dictionary of index-to-value mappings",
            "A sorted version of the iterable"
        ],
        "answer": 1,
        "explanation": "enumerate() wraps an iterable and yields (index, value) tuples, useful for looping with a counter."
    },
    {
        "question": "Which is the correct syntax for a lambda function?",
        "options": [
            "lambda x => x + 1",
            "lambda(x): x + 1",
            "lambda x: x + 1",
            "def lambda(x): return x + 1"
        ],
        "answer": 2,
        "explanation": "Lambda syntax is: lambda parameters: expression. No parentheses, def, or return keyword needed."
    },
    {
        "question": "What is the difference between '==' and 'is' in Python?",
        "options": [
            "They are identical in behavior",
            "'==' compares values; 'is' compares identity (same object in memory)",
            "'is' compares values; '==' compares identity",
            "'is' only works with integers"
        ],
        "answer": 1,
        "explanation": "'==' checks if two objects have equal values. 'is' checks if they are the exact same object in memory."
    },
    {
        "question": "What will list(range(2, 10, 3)) produce?",
        "options": ["[2, 5, 8]", "[2, 4, 6, 8]", "[2, 3, 4, 5, 6, 7, 8, 9]", "[3, 6, 9]"],
        "answer": 0,
        "explanation": "range(2, 10, 3) starts at 2, steps by 3, and stops before 10: 2, 5, 8."
    },
    {
        "question": "How do you catch multiple exception types in a single except clause?",
        "options": [
            "except ValueError, TypeError:",
            "except (ValueError, TypeError):",
            "except [ValueError, TypeError]:",
            "except ValueError or TypeError:"
        ],
        "answer": 1,
        "explanation": "Multiple exception types are grouped in a tuple: except (ValueError, TypeError):"
    },
    {
        "question": "What does the zip() function do?",
        "options": [
            "Compresses files in Python",
            "Sorts two lists simultaneously",
            "Pairs elements from multiple iterables into tuples",
            "Merges two dictionaries"
        ],
        "answer": 2,
        "explanation": "zip() takes multiple iterables and returns an iterator of tuples pairing their elements by position."
    },
    {
        "question": "What keyword turns a Python function into a generator?",
        "options": ["return", "yield", "generate", "async"],
        "answer": 1,
        "explanation": "'yield' pauses the function and returns a value; the function resumes on next()."
    },
    {
        "question": "Which built-in function returns the memory address of a Python object?",
        "options": ["ref()", "addr()", "id()", "mem()"],
        "answer": 2,
        "explanation": "id() returns the identity (memory address) of an object in CPython."
    },
    {
        "question": "What does *args capture in a function definition?",
        "options": [
            "Keyword arguments as a dict",
            "Positional arguments as a tuple",
            "All arguments as a list",
            "Only integer arguments"
        ],
        "answer": 1,
        "explanation": "*args collects extra positional arguments into a tuple. **kwargs handles keyword arguments."
    },
    {
        "question": "Which data structure guarantees O(1) average-case lookup?",
        "options": ["list", "tuple", "dict", "deque"],
        "answer": 2,
        "explanation": "dict (hash map) uses hashing for O(1) average lookup. Lists require O(n) linear search."
    },
    {
        "question": "What is the output of: bool(0), bool(''), bool([0])?",
        "options": [
            "False, False, False",
            "False, False, True",
            "True, False, True",
            "False, True, False"
        ],
        "answer": 1,
        "explanation": "0 and '' are falsy. [0] is a non-empty list — truthy even if its element is 0."
    },
    {
        "question": "What does list comprehension [x**2 for x in range(5) if x % 2 == 0] produce?",
        "options": ["[0, 4, 16]", "[1, 9, 25]", "[0, 2, 4]", "[0, 1, 4, 9, 16]"],
        "answer": 0,
        "explanation": "Filters even x (0, 2, 4) then squares them: [0, 4, 16]."
    },
    {
        "question": "Which module provides high-precision decimal arithmetic in Python?",
        "options": ["math", "fractions", "decimal", "numbers"],
        "answer": 2,
        "explanation": "The 'decimal' module provides arbitrary-precision fixed-point and floating-point arithmetic."
    },
    {
        "question": "What is the time complexity of Python's list.append()?",
        "options": ["O(n)", "O(log n)", "O(1) amortized", "O(n²)"],
        "answer": 2,
        "explanation": "append() is O(1) amortized. Occasional resizes are O(n) but averaged out over many appends."
    },
    {
        "question": "What does the 'with' statement ensure when opening a file?",
        "options": [
            "The file is read-only",
            "The file is closed automatically via __exit__",
            "The file is loaded into memory",
            "The file is locked for concurrent access"
        ],
        "answer": 1,
        "explanation": "'with' uses the context manager protocol (__enter__/__exit__), ensuring cleanup even on exceptions."
    },
    {
        "question": "Which of these is an immutable sequence type in Python?",
        "options": ["list", "bytearray", "tuple", "dict"],
        "answer": 2,
        "explanation": "Tuples are immutable — you cannot modify them after creation. Lists and bytearrays are mutable."
    }
]
